services:
  app:
    build: .
    ports:
      - "80:8080"
    environment:
      - APP_USER=There
      - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      - redis

  redis:
    image: redis:8
    volumes:
      - redis-data:/data

volumes:
  redis-data: